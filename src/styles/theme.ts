export enum COLORS {
  PINKTHEME = '#F11A7B',
}

export enum SIZES {
  MAX_WIDTH = '1290px',
}

const theme = {
  colors: {
    black: '#000e1a',
    white: '#fff',
    blue: '#007ce0',
    pinkTheme: COLORS.PINKTHEME,
  },
  space: [0, 8, 16, 24, 32, 64, 128, 256, 512],
};

export default theme;
